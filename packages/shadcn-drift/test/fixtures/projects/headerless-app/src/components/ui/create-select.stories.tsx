import { CreateSelect } from '#/components/ui/create-select.tsx'

/**
 * Storybook stories are the same case as tests — a file the shadcn CLI would
 * never have written, sitting in a directory it mostly owns.
 */
export default { title: 'ui/CreateSelect', component: CreateSelect }

export const Default = { args: {} }
